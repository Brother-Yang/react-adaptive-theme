# 默认语言设置
# 可选值: zh-CN, en-US
VITE_DEFAULT_LANGUAGE=zh-CN

# 其他环境变量可以在这里添加
# VITE_API_BASE_URL=https://api.example.com
# VITE_APP_VERSION=1.0.0

# 注意：
# 1. 以 VITE_ 开头的变量会被暴露到客户端
# 2. 敏感信息（如API密钥）不应以 VITE_ 开头
# 3. 生产环境请使用 .env.production 文件
# 4. 本地开发可使用 .env.local 文件（已在.gitignore中排除）